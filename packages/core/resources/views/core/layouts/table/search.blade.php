<div class="input-icon">
    <input type="text" form="filters" name="search" class="form-control input-sm padder" value="{{ request('search') }}" placeholder="{{ __('Tìm kiếm') }}">
    <div class="input-icon-addon">{{ get_feather_icon('search') }}</div>
</div>
